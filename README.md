# .codex

> 直接作为 `~/.codex` 使用，实现一次配置、多机同步。

## 快速部署

```bash
# 1. 备份已有配置
mv ~/.codex ~/.codex.bak 2>/dev/null || true

# 2. 克隆仓库
git clone https://github.com/randomseed713/.codex.git ~/.codex

# 3. 进入目录检查配置
cd ~/.codex
```
